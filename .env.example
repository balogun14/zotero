# Server
PORT=3000
NODE_ENV=development

# AI agent authentication (generate a strong random key)
AI_API_KEY=your-very-secret-api-key-here

# Optional: restrict frontend origin in production
# CORS_ORIGIN=https://your-domain.com

# Optional: use a custom database path
# DATABASE_URL=file:./data/library.db
